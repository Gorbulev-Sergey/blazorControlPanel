@page "/tags"
@inject ITagsService TagsService  

<TagsCreator tags="tags" удалить="удалить"></TagsCreator>

<TagsSearcher tags="tags"></TagsSearcher>


@code {
    List<tag> tags = new List<tag>();

    protected override void OnInitialized()
    {
        tags = TagsService.tags();
    }

    void удалить(tag tag)
    {
        
    }
}
